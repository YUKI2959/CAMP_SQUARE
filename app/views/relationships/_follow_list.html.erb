<% if users != [] %>
  <% users.each do |user| %>
    <table>
      <tbody>
        <div class="friend-inner">
          <div class="friend-left">
            <% if user.image.present? %>
              <%= link_to image_tag(user.image, class: :friend__image ),user_path(user.id) %>
            <% else %>
              <%= link_to image_tag('/noimage.jpg', class: :friend__image ),user_path(user.id) %>
            <% end %>
            <div class="friend-spece">
              <%= link_to user.nickname,user_path(user.id) %>
              フォロー数: <%= user.followings.count %>
              フォロワー数: <%= user.followers.count %>
            </div>
          </div>
          <div class="frien-light">
            <% if current_user.following?(user) %>
              <%= link_to "フォロー外す", user_relationships_path(user.id), method: :delete, class: :user__show__btn %>
            <% else %>
              <%= link_to "フォローする", user_relationships_path(user.id), method: :post, class: :user__show__btn %>
            <% end %>
          </div>
        </div>
      </tbody>
    </table>
  <% end %>
<% else %>
  <p>ユーザーはいません</p>
<% end %>

